from ..models.libraries import *

def define_models():
    algorithms = []
    models = {}
    for name, defined_models in algorithms:
        models[name] = []
        for model in models:
            models.get(name).append(model)
    return models


